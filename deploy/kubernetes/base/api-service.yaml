apiVersion: v1
kind: Service
metadata:
  name: elearning-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: elearning-api
  ports:
    - name: http
      port: 80
      targetPort: http
